# Pre-built frontend: build on host, COPY dist/ into nginx
# Host build: cd frontend && npm run build
FROM nginx:alpine
COPY frontend/dist /usr/share/nginx/html
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
